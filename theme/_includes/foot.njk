<footer class="foot">
  <span>© {{ build.generatedYear }} {{ profile.name }}</span>
  {%- if stats.totalItems %}<span>{{ stats.totalItems }} entries</span>{% endif %}
  {%- if stats.totalWords %}<span>{{ stats.totalWords }} words</span>{% endif %}
  {%- if taxonomy.totalUniqueSkills %}<span>{{ taxonomy.totalUniqueSkills }} skills</span>{% endif %}
</footer>
